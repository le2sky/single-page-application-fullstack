{
  "name": "modern-web-development",
  "version": "1.0.0",
  "description": "Develop a modern web using express for Node.js, Vue.js, and mongoDB.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development node ./be/bin/www",
    "serve": "cd fe && npm run serve",
    "pr": "cd fe && npm install && npm run build && cd ../be && cross-env NODE_ENV=production PORT=80 node ./bin/www",
    "pm2": "cd fe && npm install && npm run build && cd ../be && npm install && cd .. && pm2 start --env pr"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/homeless-leesky/Modern-web-development.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/homeless-leesky/Modern-web-development/issues"
  },
  "homepage": "https://github.com/homeless-leesky/Modern-web-development#readme",
  "dependencies": {
    "cross-env": "^7.0.0",
    "moment": "^2.24.0"
  }
}
